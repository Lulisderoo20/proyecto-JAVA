package com.empresa.seguridad.model;

// `enum` se usa cuando solo existe un conjunto fijo de valores posibles.
// Eso evita manejar textos sueltos y reduce errores de escritura.
// Aqui define los roles disponibles en el sistema.
public enum Role {
    ADMIN,
    USER
}
